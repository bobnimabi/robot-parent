1.出款项目暂时不考虑一下2点
    a.高并发=>由单线程抓取数据
    b.分布式=>暂时只部署一台服务器

2.出款的检查点
    不出的情况：
        1.10分钟内同一会员不出
        2.过滤重复订单（重要）
        3.金额不在min-max的范围内的不出
        4.备注栏有内容的不出
        5.首次出款的不出
        6.出款层级未勾选的不出
    那么：
        出款前：
            1.同一会员去重（一次查出同会员多个订单） + 同一会员出款标志
            2.订单去重（正常情况下不存在,暂时不做） + 已出款订单标志（重要）
            3.金额不在min-max的范围内的不出
            4.备注栏有内容的不出
            5.首次出款的不出
            6.出款层级未勾选的不出

        出款后：
            1.添加会员出款标志
            2.添加已出款订单
